# Gamification FastAPI (uvicorn or Docker host port). Vite dev proxies /admin, /wallet, etc. here.
# Use 127.0.0.1 to avoid Windows localhost → IPv6 (::1) mismatches with uvicorn.
VITE_GAMIFICATION_API_PROXY=http://127.0.0.1:8002
